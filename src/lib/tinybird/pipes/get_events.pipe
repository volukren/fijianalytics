TOKEN 'read_token' READ

NODE endpoint
DESCRIPTION >
    Get events filtered by site_id

SQL >
    %
    SELECT *
    FROM events
    WHERE
        {% if defined(site_id) %}
            site_id = {{ String(site_id) }}
        {% else %}
            1 = 1
        {% end %}
    ORDER by timestamp DESC
